<h1><%= @task.title %></h1>

<h3><%= @task.details %></h3>
<p>Is this task completed? <BR>
<%= @task.completed %></p>


<%= link_to 'Edit this task', edit_task_path %> <BR>
<%= link_to 'Delete this task',
    task_path(@task),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    %>
<%= link_to 'Back to Tasks', tasks_path %>
